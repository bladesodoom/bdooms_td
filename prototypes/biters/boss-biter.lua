-- Boss biters. Each boss type is defined in its own file under bosses/;
-- add a new require() here to register another one.

require("prototypes.biters.bosses.swarm-lord")
require("prototypes.biters.bosses.hive-lord")
